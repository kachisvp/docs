# docs

## Commands

```
git switch main
git pull origin main
git merge develop
git push origin main
```

## TODO

### awscli, SSO

### Windows_Ubuntu_Git.md

commands追加

### Windows_TypeScript.md

### Windows_Node.md

PATH
C:\Users\User\dev\nodejs\

### Windows_Python.md

**FIXME move pyenv settings**

```
printenv PYENV_ROOT PATH
cat ~/.bashrc
echo -e "\n# pyenv settings" >> ~/.bashrc
echo 'export PYENV_ROOT=$HOME/.pyenv' >> ~/.bashrc
echo 'export PATH=$PYENV_ROOT/bin:$PATH' >> ~/.bashrc
echo 'eval "$(pyenv init -)"' >> ~/.bashrc
cat ~/.bashrc
source ~/.bashrc
printenv PYENV_ROOT PATH
```

pyenv-win

### Git_Remote_CodeCommit

Ubuntuを開き、以下のコマンドを実行

```
git clone https://github.com/kachisvp/fasse_infra.git
```

> 以下が表示されること

```

```
